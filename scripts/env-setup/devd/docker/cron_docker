# Creates cron job that sends all uploaded files to Swift object storage.
FROM ubuntu:latest

RUN apt-get update && apt-get -y install cron
RUN apt-get -y install mongodb-clients
RUN apt-get -y install git
RUN apt-get -y install python-swiftclient

WORKDIR /root
RUN     git clone -b devd http://github.com/dudek313/palsweb.git

COPY crontab /etc/cron.d/swiftcron

# Give execution rights on the cron job
RUN chmod 0644 /etc/cron.d/swiftcron

# Run the command on container startup
CMD cron
